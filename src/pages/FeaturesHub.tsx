import { Navigate } from "react-router-dom";

// Features Hub folded into The Sanctuary — the single source of truth for every feature.
const FeaturesHub = () => <Navigate to="/sanctuary" replace />;

export default FeaturesHub;
